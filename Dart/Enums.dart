void main() {
  //! Enums in Dart
  //? Enums are  used to represent a fixed numbers of constant values
  /*
  enum name{
    value1, // o element value 
    value2,
    value3
  }  
  */

  for (var b in Data.values) {
    print(b);
  }
}

enum Data { my, name, bhavin, pathak }
